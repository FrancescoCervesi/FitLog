# Dipendenze del progetto FitLog
# Installa con: pip install -r requirements.txt
# Genera con:   pip freeze > requirements.txt

Flask==3.0.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.48
Flask-Login==0.6.3
Werkzeug==3.0.3
python-dotenv==1.0.1
gunicorn==22.0.0

# Driver PostgreSQL — necessario per il database persistente su Render.
psycopg2-binary==2.9.9